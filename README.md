# tping / tping3 / tping2 / tping4

> Набор инструментов для **нагрузочного тестирования собственных серверов**.

| Инструмент | Уровень | Назначение |
|------------|---------|------------|
| **tping**  | L3/L4   | UDP, IPSO, ICMP |
| **tping3** | L7      | HTTP/HTTPS, Slowloris, RUDY, HTTP/2 |         | 
| **tping2** | L4      | low-level packets |

> ⚠️ **Важно:** инструменты предназначены **только для тестирования собственных серверов** или систем, на которые у вас есть **письменное разрешение владельца**.

---

## Содержание

1. [Что такое нагрузочное тестирование](#что-такое-нагрузочное-тестирование)
2. [Возможности](#возможности)
3. [Режимы tping](#режимы-tping)
4. [Режимы tping3](#режимы-tping3)
5. [Установка на Linux](#установка-на-linux)
6. [Установка на Windows](#установка-на-windows)
7. [Установка на macOS](#установка-на-macos)
8. [Использование tping](#использование-tping)
9. [Использование tping3](#использование-tping3)
10. [Формат пакета IPSO](#формат-пакета-ipso)
11. [Прокси и режим -ddos](#прокси-и-режим--ddos)
12. [Ограничения](#ограничения)
13. [Разрешения (capabilities)](#разрешения-capabilities)
14. [Дисклеймер](#дисклеймер)
15. [Лицензия](#лицензия)

---

## Что такое нагрузочное тестирование

**Нагрузочное тестирование** (load testing) — это проверка того, как сервер ведёт себя под высокой нагрузкой. Цель — понять пределы его устойчивости:

- сколько запросов он держит,
- при каком уровне начинает отвечать медленно,
- когда возвращает **HTTP 503 Service Unavailable** или перестаёт отвечать.

Это **не атака**, а **инженерный тест**. Используется администраторами и пентестерами, чтобы:

- убедиться, что сервер выдержит наплыв пользователей,
- проверить работу anti-DDoS, WAF, CDN, rate-limiter,
- оценить, нужен ли апгрейд железа или канала.

---

## Возможности

### tping (сетевой уровень)

| Возможность | Описание |
|-------------|----------|
| UDP-пакеты | До 65507 байт |
| IPSO | Собственный формат (флаг `-ipso`) |
| ICMP | Echo-запросы (флаг `-icmp`) |
| Многопоточность | До 256 потоков |
| Автостоп | По времени (`-d SEC`) и количеству (`-c N`) |
| Статистика | Байты, пакеты, потери, MB/s, pps |
| Режимы | `-dos` / `-ddos` |
| Таблица | `-table` |

### tping3 (прикладной уровень)

| Возможность | Описание |
|-------------|----------|
| GET-флуд | HTTP GET-запросы |
| POST-флуд | HTTP POST-запросы |
| HTTP/2 | Мультиплексирование |
| Slowloris | Медленное занятие TCP |
| RUDY | Slow POST |
| Mix | Всё вместе |
| Многопоточность | До 512 потоков |
| Статистика | RPS и коды 200/429/503/timeout |
| UA-файл | Загрузка списка User-Agent |
| Режимы | `-dos` / `-ddos` |
| Прокси | Файл или GitHub |

###  tping2 ( серверный уровень)
| Режим Флаг | Что делает |
|-------------|-----------|
| TCP SYN -syn SYN-пакеты | (классический SYN-flood) |
| TCP ACK -ack | ACK-пакеты |
| TCP FIN -fin | FIN-пакеты |
| TCP RST -rst | RST-пакеты |
| TCP XMAS -xmas | FIN+PSH+URG |
| UDP -udp | UDP-пакеты |
| ICMP -icmp | Echo-запросы |
| IPSO -ipso | Наш формат поверх UDP |
| Подмена src -spoof | Случайный src IP |
| Фрагментация -frag | Дробить пакеты |
| Вывод RTT | (авто) Время ответа для каждого пакета | 
| DoS/DDoS -dos / -ddos | Свой IP / прокси |
---

## Режимы tping

| Режим | Флаг | Что делает |
|-------|------|-----------|
| UDP | (по умолч.) | Отправляет UDP-пакеты |
| IPSO | `-ipso` | UDP + собственный заголовок IPSO |
| ICMP | `-icmp` | Echo-запросы (нужен root) |
| DoS | `-dos` | Отправка с локального IP |
| DDoS | `-ddos` | Отправка через прокси |

---

## Режимы tping3

| Режим | Флаг | Что делает |
|-------|------|-----------|
| GET | `-mode get` | HTTP GET-флуд |
| POST | `-mode post` | HTTP POST-флуд |
| HTTP/2 | `-mode http2` | HTTP/2-мультиплексирование |
| Slowloris | `-mode slowloris` | Медленные соединения |
| RUDY | `-mode rudy` | Slow POST |
| Mix | `-mode mix` | Всё вместе |
| DoS | `-dos` | Отправка с локального IP |
| DDoS | `-ddos` | Отправка через прокси |

---

## Установка на Linux

### Ubuntu / Debian / Kali

```bash
sudo apt update
sudo apt install -y g++ libcurl4-openssl-dev libssl-dev libcap-dev curl libssl-dev golang
git clone https://github.com/qopix/tping.git
cd tping
make
```
## Tping / tping3
# Таблица Размеров
Обозначение Значение
b байты
kb килобайты
mb мегабайты
gb гигабайты
t терабайты

# Флаги Tping
Флаг Описание
-dos отправка с локального IP (по умолчанию)
-ddos отправка через прокси
-ipso формат IPSO поверх UDP
-icmp ICMP-режим (нужен root)
-l бесконечная отправка
-t <N> число потоков (1..256)
-d <SEC> длительность в секундах
-c <N> пакетов на поток
-p <PORT> порт (по умолчанию 80)
-table таблица размеров
-help справка

# Флаги Tping3
Флаг Описание
-dos отправка с локального IP (по умолчанию)
-ddos отправка через прокси
-url <URL> целевой URL
-mode режим работы (по умолчанию get)
-t <N> число потоков (1..512)
-d <SEC> длительность в секундах
-ua <FILE> файл User-Agent

# Формат пакета IpSo
Поле Размер Описание
magic 4 "IPSO"
seq 4 номер пакета
frag_id 4 ID потока
total_size 8 заявленный объём
timestamp 8 время отправки (µs)
payload_len 2 длина payload
payload N данные

#tping2
Режим Флаг Что делает
TCP SYN -syn SYN-пакеты (классический SYN-flood)
TCP ACK -ack ACK-пакеты
TCP FIN -fin FIN-пакеты
TCP RST -rst RST-пакеты
TCP XMAS -xmas FIN+PSH+URG
UDP -udp UDP-пакеты
ICMP -icmp Echo-запросы
IPSO -ipso Наш формат поверх UDP
Подмена src -spoof Случайный src IP
Фрагментация -frag Дробить пакеты
Вывод RTT (авто) Время ответа для каждого пакета
DoS/DDoS -dos / -ddos Свой IP / прокси

## Makefile
сборка всех инструментов-make.                        
сбора по отдельрости:                                 
make tping.                                             
make tping2.                                            
make tping3.                                            
Установка в систему:                                   
sudo make install.                                     
очистка:                                                
make clean.                                              

## tping4
> Набор инструментов для **нагрузочного тестирования собственных серверов**.

Инструмент | Уровень | Назначение
---|---|---
**tping**  | L3/L4 | UDP, IPSO, ICMP
**tping3** | L7    | HTTP/HTTPS, Slowloris, RUDY, HTTP/2
**tping4** | L7    | Slowloris (медленное занятие TCP)
**tping2** | L4    | low-level packets

> ⚠️ **Важно:** инструменты предназначены **только для тестирования собственных серверов** или систем, на которые у вас есть **письменное разрешение владельца**.

---

## Содержание

1. Что такое Slowloris
2. Возможности
3. Режимы tping4
4. Установка на Linux
5. Установка на macOS
6. Установка на Windows (WSL)
7. Использование tping4
8. Флаги tping4
9. Ограничения
10. Разрешения (capabilities)
11. Дисклеймер
12. Лицензия

---

## Что такое Slowloris

**Slowloris** — техника нагрузочного тестирования уровня L7, при которой клиент
открывает множество TCP-соединений к HTTP-серверу и отправляет заголовки
запроса **очень медленно**, никогда не завершая запрос символом `\r\n\r\n`.

Сервер вынужден держать каждое соединение в ожидании — воркер занят,
новые легитимные клиенты не обслуживаются. Цель теста — выяснить:

- сколько параллельных «медленных» соединений держит сервер;
- через какое время срабатывает `client_header_timeout`;
- помогает ли reverse proxy / WAF / rate-limiter;
- когда сервер начинает отдавать **503 Service Unavailable**.

Это **не атака**, а **инженерный тест** устойчивости конфигурации.

---

## Возможности

### tping4 (L7 / Slowloris)

Возможность        | Описание
---|---
Slowloris          | Медленная отправка HTTP-заголовков
Многопоточность    | До 4096 потоков
Автостоп           | По времени (`-d SEC`)
Keep-alive         | Периодический «капельный» заголовок (`-ka MS`)
Случайный path     | `-rand-path` — рандомизация URI
Случайный UA       | `-rand-ua` — список User-Agent
Прокси             | `-ddos` — через SOCKS5/HTTP-прокси из файла
Статистика         | alive / sent / errors, обновление раз в секунду
SIGINT             | Корректная остановка по Ctrl+C

---

## Режимы tping4

Режим | Флаг    | Что делает
---|---|---
DoS   | `-dos`  | Отправка с локального IP (по умолчанию)
DDoS  | `-ddos` | Отправка через прокси
Random| `-rand` | Рандомизация path и User-Agent

---